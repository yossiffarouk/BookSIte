@model UserMangmentVM


<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Manage User Role</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <form method="post" class="row">
            <input asp-for="ApplicationsUser.Id" hidden />
            <div class="border p-3">
                @*<div asp-validation-summary="ModelOnly"></div>*@
                <div class="form-floating py-2 col-12">
                    <input asp-for="ApplicationsUser.Name" readonly class="form-control border-0 shadow" />
                    <label asp-for="ApplicationsUser.Name" class="ms-2"></label>
                </div>
                <div class="form-floating py-2 col-12">
                    <select asp-for="ApplicationsUser.Role" asp-items="@Model.Roles" class="form-select"></select>
                </div>
                @{
                    var companyVisible = "display:none;";
                }
                @if (Model.ApplicationsUser.Role == SD.Role_User_Company)
                {
                    companyVisible = "display:block;";
                }
                <div class="form-floating py-2 col-12">
                    <select asp-for="ApplicationsUser.CompanyId" style="@companyVisible" asp-items="@Model.Companys" class="form-select">
                    </select>
                </div>

                <div class="row pt-2">
                    <div class="col-6 col-md-3">
                        <button type="submit" class="btn btn-primary form-control">Update Role</button>
                    </div>
                    <div class="col-6 col-md-3">
                        <a asp-action="Index" class="btn btn-outline-primary border  form-control">
                            Back to List
                        </a>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>




@section Scripts {
    @{
        <partial name="_ValidationScriptsPartial" />
    }
    <script>
        $(document).ready(function () {
            $('#ApplicationsUser_Role').change(function () {
                var selection = $('#ApplicationsUser_Role Option:Selected').text();
                if (selection == 'Company') {
                    $('#ApplicationsUser_CompanyId').show();
                }
                else {
                    $('#ApplicationsUser_CompanyId').hide();
                }
            })
        })
    </script>
}